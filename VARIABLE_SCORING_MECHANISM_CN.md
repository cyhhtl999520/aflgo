# 关键变量识别的变量评分机制

## 概述

本文档描述了GFuzz关键变量识别算法中使用的综合多维评分机制。评分系统从六个维度评估每个变量，以确定其在定向模糊测试期间监控的重要性。

## 动机

并非所有变量对检测漏洞都同等重要。原则性的评分机制有助于：
1. **降低监控开销**，专注于高影响力变量
2. **提高检测效果**，优先考虑与漏洞相关的变量
3. **平衡覆盖率**，涵盖不同类型和上下文的变量
4. **提供可解释排名**，便于调试和分析

## 评分维度

### 1. 距离分数（权重：0.25）

**定义**：衡量变量在程序结构中距离目标位置的远近程度。

**组成部分**：
- **调用图距离**：从变量所在函数到目标函数的函数调用数量
- **控制流图距离**：从变量定义到目标位置的基本块数量

**公式**：
```
距离分数(v) = α · (1 - d_cg(v,T) / d_cg_max) + (1-α) · (1 - d_cfg(v,T) / d_cfg_max)
```

其中α = 0.6（调用图距离的权重）

**直觉**：距离目标更近的变量更可能影响目标的可达性和行为。

### 2. 类型复杂度分数（权重：0.20）

**定义**：根据变量类型在漏洞中的常见程度分配分数。

**类型层次结构**（分数范围0.0 - 1.0）：
1. **指针**（1.0）：对内存安全最关键
2. **字符串**（0.85）：常见于缓冲区溢出
3. **数组**（0.75）：大小相关漏洞
4. **结构体/对象**（0.65）：聚合类型
5. **浮点数**（0.50）：精度相关错误
6. **整数**（0.40）：算术漏洞
7. **字符**（0.30）：影响有限
8. **布尔值**（0.20）：复杂度最低

**修饰符**：
- 无符号类型：+0.10
- volatile类型：+0.05
- 指向非常量数据的常量指针：+0.15

### 3. 内存上下文分数（权重：0.20）

**定义**：评估变量在内存相关操作中的参与程度。

**评分操作**：
1. **分配**（每次0.30）：malloc、calloc、realloc、new
2. **释放**（每次0.25）：free、delete
3. **内存复制**（每次0.20）：memcpy、strcpy、strncpy、memmove
4. **内存比较**（每次0.10）：memcmp、strcmp、strncmp
5. **大小计算**（每次0.15）：sizeof、strlen、大小计算

**公式**：
```
内存上下文分数(v) = min(1.0, Σ weight(op) for op in 内存操作(v))
```

**饱和**：上限为1.0以防止过度加权

### 4. 控制流敏感性分数（权重：0.15）

**定义**：衡量变量影响控制流决策的频率。

**上下文**：
1. **条件分支**（每次0.25）：if、switch条件
2. **循环条件**（每次0.30）：while、for、do-while条件
3. **循环边界**（每次0.20）：循环初始化、递增、终止
4. **函数调用守卫**（每次0.15）：关键函数调用前的条件
5. **返回语句**（每次0.10）：变量影响返回值

**嵌套乘数**：每个嵌套级别增加0.05（最多+0.20）

### 5. 数据流影响分数（权重：0.15）

**定义**：通过定义-使用链评估变量对其他变量的影响。

**指标**：
1. **直接使用**：直接从该变量赋值的变量
2. **间接使用**：传递依赖于该变量的变量
3. **关键路径长度**：到目标位置的最长定义-使用链
4. **扇出**：该变量影响的变量数量

**公式**：
```
数据流影响分数(v) = β₁·直接使用(v)/最大直接使用 
                   + β₂·间接使用(v)/最大间接使用
                   + β₃·关键路径(v)/最大路径长度
                   + β₄·扇出(v)/最大扇出
```

其中β₁=0.30，β₂=0.25，β₃=0.25，β₄=0.20

### 6. 频率分数（权重：0.05）

**定义**：变量在不同执行路径中的使用频率。

**计算**：
1. 统计使用变量的基本块数量
2. 按函数中的总基本块数归一化
3. 按函数执行概率加权（如可用）

**公式**：
```
频率分数(v) = (基本块使用(v) / 总基本块) · 执行概率(func(v))
```

## 组合评分

### 加权聚合

**公式**：
```
分数(v) = w₁·距离分数(v) 
        + w₂·类型复杂度分数(v)
        + w₃·内存上下文分数(v)
        + w₄·控制流敏感性分数(v)
        + w₅·数据流影响分数(v)
        + w₆·频率分数(v)
```

**默认权重**：
```
w₁ = 0.25  (距离)
w₂ = 0.20  (类型复杂度)
w₃ = 0.20  (内存上下文)
w₄ = 0.15  (控制流敏感性)
w₅ = 0.15  (数据流影响)
w₆ = 0.05  (频率)
```

**总和**：Σwᵢ = 1.0（归一化权重）

### 阈值过滤

**分数阈值**：`分数(v) < 阈值`的变量被过滤掉

**默认阈值**：0.5（可配置）

**自适应阈值**：根据变量总数调整
- 如果`|变量| > 200`：阈值 = 0.6（更具选择性）
- 如果`|变量| < 50`：阈值 = 0.3（选择性较低）

### Top-K选择

**策略**：阈值过滤后，按分数选择前K个变量

**默认K**：100个变量（可配置）

**基于类型的最小值**：确保多样性
- 至少20%指针
- 至少15%整数
- 至少10%字符串
- 其余55%按分数排名

## 配置

### 自定义权重

**权重调优指南**：
1. **内存密集型程序**：增加w₃（内存上下文）
2. **控制流密集型程序**：增加w₄（控制流）
3. **目标局部性强**：增加w₁（距离）
4. **复杂数据结构**：增加w₂（类型复杂度）

### 阈值调优

**最优阈值寻找**：
1. 从默认值开始（0.5）
2. 运行试点模糊测试活动
3. 分析变量重要性与开销
4. 调整阈值以平衡精确度/召回率

**阈值影响**：
- **较低阈值**（0.3-0.4）：更多变量，更高召回率，更高开销
- **中等阈值**（0.5-0.6）：平衡精确度/召回率
- **较高阈值**（0.7-0.8）：更少变量，更高精确度，更低开销

## 评估指标

### 精确度

**定义**：所选变量中与漏洞相关的比例

```
精确度 = |选中 ∩ 相关| / |选中|
```

**目标**：≥ 70%

### 召回率

**定义**：相关变量中被选中的比例

```
召回率 = |选中 ∩ 相关| / |相关|
```

**目标**：≥ 90%

### F1分数

**定义**：精确度和召回率的调和平均数

```
F1 = 2 · (精确度 · 召回率) / (精确度 + 召回率)
```

**目标**：≥ 0.75

## 实验结果

### 变量集合缩减

| 程序      | 总变量数 | 选中变量数 | 缩减率  |
|-----------|---------|-----------|---------|
| mJS       | 342     | 87        | 74.6%   |
| binutils  | 1,248   | 112       | 91.0%   |
| libming   | 856     | 95        | 88.9%   |
| libxml2   | 1,124   | 103       | 90.8%   |
| **平均**  | **892.5** | **99.25** | **86.3%** |

### 精确度和召回率

| 程序      | 精确度 | 召回率 | F1分数 |
|-----------|--------|--------|--------|
| mJS       | 78.2%  | 93.5%  | 0.851  |
| binutils  | 82.1%  | 91.2%  | 0.864  |
| libming   | 75.8%  | 94.8%  | 0.842  |
| libxml2   | 80.5%  | 92.7%  | 0.861  |
| **平均**  | **79.2%** | **93.1%** | **0.855** |

### 运行时开销

| 程序      | 全部变量 | 选中变量 | 缩减率 |
|-----------|---------|---------|--------|
| mJS       | 28.4%   | 11.2%   | 60.6%  |
| binutils  | 32.1%   | 13.8%   | 57.0%  |
| libming   | 25.7%   | 12.5%   | 51.4%  |
| libxml2   | 29.8%   | 13.1%   | 56.0%  |
| **平均**  | **29.0%** | **12.7%** | **56.2%** |

## 结论

多维评分机制为关键变量识别提供了原则性方法，平衡了多个关注点：
- **相关性**：距离和控制流确保关注目标
- **影响**：类型和内存上下文优先考虑高风险变量
- **效率**：评分平均减少86%的监控集合
- **有效性**：为与漏洞相关的变量保持93%的召回率

这种方法显著改进了简单的"监控所有内容"或基于距离的过滤策略，为变量选择过程提供了理论依据和实证验证。
